query AnimeList($page: Int) {
    Page(page: $page) {
        media {
            coverImage {
                medium
            }
            title {
                english
            }
            bannerImage
        }
        pageInfo {
            hasNextPage
            currentPage
            lastPage
        }
    }
}